# Send message
POST    /message                    controllers.MessageApi.save

# Get message
GET     /message                    controllers.MessageApi.list
GET     /message/:id                controllers.MessageApi.get(id:Long)